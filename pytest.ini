[pytest]
addopts = -m "not slow and not dataset and not interactive"
markers =
    slow: slow tests (excluded by default)
    dataset: uses real datasets (excluded by default)
    interactive: requires user interaction (excluded by default)
